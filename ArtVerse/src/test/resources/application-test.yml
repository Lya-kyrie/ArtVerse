spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/manga_novel
    username: postgres
    password: postgres
  flyway:
    enabled: false

artverse:
  api-token: ""
  deepseek:
    api-key: "test-key"
  image:
    api-key: "test-key"
  minio:
    access-key: "test-key"
    secret-key: "test-secret"
